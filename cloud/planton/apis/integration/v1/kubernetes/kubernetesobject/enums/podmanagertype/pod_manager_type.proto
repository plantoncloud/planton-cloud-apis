syntax = "proto3";

package cloud.planton.apis.integration.v1.kubernetes.kubernetesobject.enums.podmanagertype;

enum PodManagerType {
  unspecified = 0;
  deployment = 1;
  stateful_set = 2;
  cron_job = 3;
}
