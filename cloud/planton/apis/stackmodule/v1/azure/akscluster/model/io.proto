syntax = "proto3";

package cloud.planton.apis.stackmodule.v1.azure.akscluster.model;

import "buf/validate/validate.proto";

// wrapper for aks-cluster id
message AksClusterId {
  string value = 1 [
    (buf.validate.field).required = true
  ];
}
