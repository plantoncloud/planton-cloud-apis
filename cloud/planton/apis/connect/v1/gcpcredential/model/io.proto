syntax = "proto3";

package cloud.planton.apis.connect.v1.gcpcredential.model;

import "buf/validate/validate.proto";

// wrapper for organization credential id
message GcpCredentialId {
  string value = 1 [
    (buf.validate.field).required = true
  ];
}
