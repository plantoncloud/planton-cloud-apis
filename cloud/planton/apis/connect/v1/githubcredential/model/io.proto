syntax = "proto3";

package cloud.planton.apis.connect.v1.githubcredential.model;

import "buf/validate/validate.proto";

// wrapper for github-credential id
message GithubCredentialId {
  string value = 1 [
    (buf.validate.field).required = true
  ];
}
