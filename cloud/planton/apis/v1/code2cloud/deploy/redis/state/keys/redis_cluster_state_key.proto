syntax = "proto3";

package cloud.planton.apis.v1.code2cloud.deploy.opensource.redis.state.keys;

//wrapper for redis-cluster id used as key in redis messages
message RedisClusterStateKey {
  string value = 1;
}
