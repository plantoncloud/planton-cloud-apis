syntax = "proto3";

package cloud.planton.apis.code2cloud.v1.connect.azurecredential.model;

import "buf/validate/validate.proto";

// wrapper for azure-credential id
message AzureCredentialId {
  string value = 1 [
    (buf.validate.field).required = true
  ];
}
