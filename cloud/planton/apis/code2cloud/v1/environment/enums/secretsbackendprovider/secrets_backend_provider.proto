syntax = "proto3";

package cloud.planton.apis.code2cloud.v1.environment.enums.secretsbackendprovider;

// environment secrets-backend provider
enum EnvironmentSecretsBackendProvider {
  ENVIRONMENT_SECRETS_BACKEND_PROVIDER_UNSPECIFIED = 0;
  gcp_secrets_manager = 1;
  aws_secrets_manager = 2;
  azure_key_vault = 3;
}
