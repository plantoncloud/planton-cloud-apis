syntax = "proto3";

package cloud.planton.apis.code2cloud.v1.kubernetes.plantoncloudkubernetes.model;

import "buf/validate/validate.proto";

//wrapper for id field of planton-cloud-kubernetes
message PlantonCloudKubernetesId {
  string value = 1 [
    (buf.validate.field).required = true
  ];
}
