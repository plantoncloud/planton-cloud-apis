syntax = "proto3";

package cloud.planton.apis.code2cloud.v1.deploy.aws.ekscluster.enums.eksworkersmanagementmode;

enum EksWorkersManagementMode {
  unspecified = 0;
  ec2 = 1;
  fargate = 2;
  managed_node_groups = 3;
}
