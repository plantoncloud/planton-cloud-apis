syntax = "proto3";

package cloud.planton.apis.code2cloud.v1.deploy.azure.akscluster.model;

import "buf/validate/validate.proto";

// wrapper for aks-cluster id
message AksClusterId {
  string value = 1 [
    (buf.validate.field).required = true
  ];
}
