syntax = "proto3";

package cloud.planton.apis.code2cloud.v1.microserviceinstance.enums.podcontrollertype;

enum PodControllerType {
  unspecified = 0;
  deployment = 1;
  stateful_set = 2;
  cron_job = 3;
}
