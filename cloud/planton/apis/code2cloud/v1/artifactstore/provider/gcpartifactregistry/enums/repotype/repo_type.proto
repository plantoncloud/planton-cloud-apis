package cloud.planton.apis.v1.code2cloud.artifactstore.provider.gcpartifactregistry.enums.repotype;

//note: the values are intentionally left capitalized as required by the gcp artifact-registry apis
enum GcpArtifactRegistryRepoType {
  GCP_ARTIFACT_REGISTRY_REPO_TYPE_UNSPECIFIED = 0;
  DOCKER = 1;
  NPM = 2;
  MAVEN = 3;
  PYTHON = 4;
  GO = 5;
}
