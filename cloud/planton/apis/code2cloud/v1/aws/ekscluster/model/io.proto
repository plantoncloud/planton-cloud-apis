syntax = "proto3";

package cloud.planton.apis.code2cloud.v1.aws.ekscluster.model;

import "buf/validate/validate.proto";

// wrapper for eks-cluster id
message EksClusterId {
  string value = 1 [
    (buf.validate.field).required = true
  ];
}
