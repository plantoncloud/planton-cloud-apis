syntax = "proto3";

package cloud.planton.apis.commons.kubernetes.enums.kubernetesworkloadingresstype;

// ingress type for workload deployed to kubernetes cluster
enum KubernetesWorkloadIngressType {
  unspecified = 0;
  load_balancer = 1;
  ingress_controller = 2;
}
