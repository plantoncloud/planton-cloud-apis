syntax = "proto3";

package cloud.planton.apis.commons.protobuf;

//this is a temporary workaround for an issue with use google's built in empty message in typescript codebase.
message CustomEmpty {
  bool is_empty = 1;
}
